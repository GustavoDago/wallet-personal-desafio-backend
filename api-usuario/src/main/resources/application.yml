server:
  port: 8800
spring:
  application:
    name: api-usuario
  config:
    import: optional:configserver:http://localhost:8888
  security:
    oauth2:
      client:
        resourceserver:
          jwt:
            issuer-uri: ${app.keycloak.serverUrl}/realms/${app.keycloak.realm}
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      prefer-ip-address: true
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
app:
  keycloak:
    admin:
      clientId: admin-cli
      clientSecret: wtjcofHqRcqmus9oOCDahU7MG9jv4GVP
    realm: spring
    serverUrl: http://localhost:9081

